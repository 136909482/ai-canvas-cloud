# setup.sh generates secrets/release.env with mode 600. Do not copy this file by hand.
# The only values supplied by an operator are the two domains, ACR repository and OSS credentials.
APP_REPOSITORY=registry.cn-hangzhou.aliyuncs.com/replace-with-namespace/ai-canvas-cloud
APP_IMAGE=
