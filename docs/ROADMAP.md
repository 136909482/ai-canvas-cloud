# AI Canvas Cloud 开发路线

本文档是唯一阶段状态入口，只保留“已完成摘要、当前剩余验收、下一阶段”。稳定架构写入 `DEVELOPMENT.md`，表和事务写入 `DATA_MODEL.md`，HTTP 契约写入 `API.md`。不要记录某次测试数量、命令输出或重复完成复盘。

## 当前状态

**P8-8 用户运营与最终安全验收已完成当前范围**。生产站点已完成首次部署并进入真实环境观察，用户管理、运营功能和生产浏览器验收均已收口。真实 staging 备份恢复演练经明确风险接受后延期，不视为通过，并转为 P9 正式运营前必须关闭的硬门槛。P9 尚未开始。

HTTP 接入层 Fastify 迁移已完成并收口为单一实现：Node 20 门禁、服务端 TypeBox/OpenAPI、严格 JSON parser、公共与 Admin 路由清单门禁、两个服务的完整 Fastify 路由组、隔离真实依赖 E2E 和多实例验收均已落地；旧 Node HTTP 入口、双适配开关和对照基准已经删除。本地、staging、production 和 single-host 均直接运行 Fastify；真实 production 首次发布已完成并继续观察，应用级回滚依赖上一版镜像而不是运行时切换。真实 staging 恢复演练按上述延期决定在正式运营前收口。

| 阶段                  | 状态           | 长期结果                                                                    |
| --------------------- | -------------- | --------------------------------------------------------------------------- |
| P0 文档与契约         | 已完成         | 建立长期文档、仓库边界和首版契约                                            |
| P1 工程骨架           | 已完成当前范围 | npm monorepo、四应用、本地 PostgreSQL/Redis/MinIO、迁移和健康检查           |
| P2 用户与个人空间     | 已完成当前范围 | Better Auth、个人 workspace、设备/session、验证与重置闭环                   |
| P3 项目图             | 已完成         | 关系图、增量保存、版本冲突、changes、checkpoint 与恢复                      |
| P4 资产治理           | 已完成         | 私有对象存储、上传确认、引用、配额、manifest 与后台预检式延迟 GC            |
| P6 本地/云端迁移      | 已完成         | 目录包预检、暂存上传、事务 commit、冲突策略与导出                           |
| P7 staging 与安全     | 工程基线完成   | 安全边界、隔离测试、指标、备份恢复和 schema 发布门禁；真实恢复演练延期至 P9 |
| P8 Admin 与浏览器生成 | 已完成当前范围 | P8-1 至 P8-8 功能与生产浏览器验收完成，恢复演练按风险接受延期               |
| P9 生产灰度           | 未开始         | 真实生产资源、合规、灰度和正式上线                                          |
| P10 团队协作          | 延后评估       | 个人空间稳定运营后再决定                                                    |

## 已完成能力

### Cloud 基础

- 普通账号使用全局唯一且不可修改的用户名、邮箱和密码；支持用户名/邮箱登录、单活跃 session、设备历史、邮箱验证/密码重置和 personal workspace，普通界面不上传或读取图片头像。
- 关系化项目图、ID 级 diff、幂等批次、乐观版本冲突、手动/定期 checkpoint 和历史恢复。
- 私有图片/视频资产、预签名上传、服务端 metadata 校验、配额、引用/checkpoint 保护，以及后台聚合预检与二次确认的 7 天延迟 GC。
- 版本化目录包的 prepare、暂存、copy/replace commit、进度重试和导出。
- 普通 API 的 CORS/CSRF/严格 JSON/Redis 限流、两账号授权边界、健康指标和脱敏诊断。

### Admin

- 独立 Admin Origin、Cookie、Better Auth Secret、数据库角色、`admin` schema、RBAC、可选验证码和不可修改审计。
- 版本化网站设置、Logo/Favicon 受控上传、公开最小投影。
- 用户列表/详情、封禁、解封、session 撤销，以及注册、生成请求/结果/质量、存储和依赖健康聚合。
- 超级管理员可测试、版本化发布、即时启停全站 SMTP；凭据使用独立部署密钥加密，普通验证/重置邮件按 revision 动态生效。
- 超级管理员可在不查看用户文件明细的前提下扫描并清理无引用 OSS 资产，操作只记录聚合审计。
- `super_admin` 与 `operator` 可创建、编辑、发布和下线全站通知；用户通知中心合并本地运行消息与云端未读公告，并提供可回看的发布时序时间线。
- Admin 不读取项目正文、Prompt、资产内容/object key、session token 或浏览器 Provider 配置。

### 浏览器本地生成

- 独立室内设计节点 V1 已落地：六套原创光影预设、完整室内参数、确定性 JSON 编译，以及可联动、可编辑并能继续连接通用 AI 绘图的文本输出；图片输入、模型选择、任务队列和结果入云继续由通用 AI 绘图节点负责，景观设计与建筑设计节点仍未开始。
- 加密设备 Vault v3：Provider、独立 Key 槽、模型条目、匿名绑定和版本化自定义图片 Provider Manifest，兼容迁移 v2；本地任务缓存 v4 兼容迁移 v3，并加密暂存待入云图片结果。
- 受控 OpenAI Compatible/DashScope chat、image、video 协议；标准图片服务商配置默认固定为 Bearer + 同步，底层 OpenAI 异步 adapter 暂保留；特殊接口使用声明式 `custom-http-image-v1` 同步/轮询 Manifest。平台不代理 Provider，也不接收用户 Key、endpoint、真实模型 ID、Manifest 或远程任务 ID。
- 服务商模型发现、服务商下模型管理、节点按模型分组并选择具体服务商路由，以及不可执行状态提示。
- 云端图只保存 `local:<uuid>`，新设备由用户显式绑定，不自动按名称或上游 ID 替换。
- 画布管理和外观设置的九项非敏感偏好按账号与工作区同步；旧本地配置只在云端未初始化时迁移，Provider Vault 与本地任务继续保持设备隔离。
- 统一浏览器任务中心按 FIFO 原子领取任务，图片并发 8、视频并发 1；同步/异步图片走受控 adapter，同一节点每次生成独立结果，异步任务和待入云结果可在同设备恢复。
- 媒体结果经 Cloud 资产上传和幂等图变更入云；Provider POST 不自动重试，轮询 GET 有限退避，上传失败不重新生成。Provider 私有配置、绑定指纹、任务阶段、remote task ID 和临时 Blob 均不进入 Cloud。
- 文本/图片/视频调用发送不含 Provider 私有信息的有限 attempt 遥测，Admin 按上海自然日展示今日 KPI、7 日趋势、运行质量和失败分类。

## P8-8：验收结论与延期项

功能实现与生产站点首次部署已完成：用户运营、生成运营仪表盘、带原因的账号操作、session 撤销、审计、服务商模型发现和节点模型选择均已落地。

生产环境已完成 Provider/模型发现与导入、模型分组与服务商路由、匿名引用绑定、未绑定/缺失/停用降级，以及图像/聊天/视频生成的人工验收；注册用户名校验、用户名/邮箱登录、会话接管、云项目与媒体重新加载和 390px 关键界面也已通过。API 依赖 readiness、公网指标隐藏、匿名授权边界、安全响应头和 EdgeOne 不可变静态资源缓存符合预期。两个真实账号在独立浏览器存储上下文中的身份、Cloud 项目与资产、Provider/Vault 和任务缓存隔离已通过；真实 Admin 与公网 SMTP 的连接、投递、验证、密码重置、停启和密钥轮换也已完成人工验收。

明确延期项：

- 准备真实 `infra/deploy/staging/staging.env` 和可用 Docker 环境，运行 staging 配置门禁、部署、备份与隔离恢复演练。
- 在上述环境完成发布门禁、依赖故障、凭据脱敏和恢复一致性验收，并保留可复现证据。

该延期只解除对 P8-8 当前范围的阻塞，不代表灾难恢复能力已验证。正式运营前必须完成真实环境演练；不能用本地 fake 环境替代，也不能在完成前对外声明备份可恢复。

## P9：生产灰度与正式上线

P8-8 当前范围完成后进入 P9。P9 尚未开始；启动后应先关闭上述恢复演练延期项，再推进灰度与正式运营。交付范围：

- 正式域名、HTTPS、Web CDN、API/Admin、PostgreSQL、Redis、私有对象存储和邮件服务。
- 平台密钥管理、环境隔离、邀请/灰度开关、存储配额和紧急停写能力。
- 站内用户协议、隐私政策、帮助中心、问题反馈和注册强制同意已落地；正式上线前仍需运营主体完成法律审校，并补齐数据导出、账号删除、备案、内容安全与适用合规检查。
- 发布、回滚、故障公告和责任人流程。

上线门槛：

- 两个真实账号在不同浏览器完成端到端隔离验证。
- 生产浏览器生成不依赖 Vite 代理，用户 Key 只存在设备 Vault，Cloud 请求不携带 Provider 私有配置。
- 自动备份、隔离恢复和对象误删恢复策略验证通过。
- 同设备异步恢复、页面中断、失败重试和结果上传幂等通过。
- 应用回滚能读取发布前数据；不可逆 migration 不与首发应用同时发布。

## P10：团队与协作

个人空间稳定运营后再按真实需求评估团队邀请、角色 UI、只读分享、评论、项目锁和实时协作。实时协作需要单独选择串行命令或 CRDT 协议，不在 P9 前提前实现。
